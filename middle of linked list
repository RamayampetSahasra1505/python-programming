class Solution:
    def getMiddle(self, head):
        temp=head
        count=0
        while temp:
            count+=1
            temp=temp.next
        mid_value=count//2
        temp=head
        for i in range(mid_value):
            temp=temp.next
        return temp.data
